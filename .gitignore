node_modules/
.next/
.claude/
out/
build/

# Local environment — contains your node's RPC credentials. Never commit.
.env.local
.env*.local

# Runtime data written by the poller.
data/*.jsonl
data/*.json
!data/.gitkeep

# Responses recorded from a real node by `npm run probe`. Kept out of the repo
# because they say where your endpoint is and what it answered. Demo mode does
# not need them — it is generated by lib/rpc/demo.ts.
lib/rpc/fixtures/recorded/

*.tsbuildinfo
next-env.d.ts
.DS_Store
